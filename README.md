# etylizer browser editor

A self-contained editor that runs the **etylizer** Erlang/Elixir set-theoretic
typechecker entirely in the browser, on a wasm BEAM built from the separate
`erlang-otp-wasm` repo. 

Both report the first type error (early-exit). 
